# 更新书目、书评与旅行照片

网站的可替换内容集中在 `app/room-content.ts`。

## 修改书评

在 `readingLibrary` 或 `professionalLibrary` 中找到对应书目，替换 `note` 后面的文字：

```ts
{
  title: "黄金时代",
  author: "王小波",
  category: "小说 · 中国文学",
  note: "把你的真实书评写在这里。需要分段时，可以使用两个换行。",
  colors: ["#d6aa43", "#7a3b23"]
}
```

`colors` 是文字封面的两种渐变色，不需要真实封面图片。

## 添加旅行照片

1. 把照片复制到 `public/travel/`。建议使用简短的英文文件名，例如 `kyoto-2025.jpg`。
2. 在 `app/room-content.ts` 的 `travelGallery` 中填写照片信息：

```ts
{
  title: "雨后的京都",
  location: "日本 · 京都",
  year: "2025",
  image: "/travel/kyoto-2025.jpg"
}
```

没有填写 `image` 的项目会继续显示为照片占位框。照片建议使用 JPG 或 WebP，横向照片效果最好。
